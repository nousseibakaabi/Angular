import { HeilightDirective } from './heilight.directive';

describe('HeilightDirective', () => {
  it('should create an instance', () => {
    const directive = new HeilightDirective();
    expect(directive).toBeTruthy();
  });
});
